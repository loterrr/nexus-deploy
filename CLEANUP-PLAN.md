# Project Cleanup Plan (Dry Run)

**Project Root**: `c:\Users\loter\Desktop\nexus_v1`  
**Date & Time**: `2026-09-15 20:20:00`  
**Framework / Stack**: `Next.js 14.2.35 (App Router), React 18, WebLLM (@mlc-ai/web-llm), Transformers.js, Electron`  
**Package Manager**: `npm`  
**Status**: `PENDING CONFIRMATION`

---

## Executive Summary

- **Total Files Scanned**: `20,553` (including 20,440 files in untracked Python `venv/`)
- **Total Directories Scanned**: `1,753`
- **Total Candidates Identified**: `3`
- **Estimated Space Reclaimed**: `~719.5 MB` (if `venv/` is removed upon review)

---

## 1. SAFE TO DELETE

Files that are demonstrably unnecessary, safe to remove immediately without affecting builds, tests, or runtime.

| Path | Category | Reason | Evidence | Risk |
| :--- | :--- | :--- | :--- | :--- |
| `src/lib/onnxruntime-node-mock.js` | OBSOLETE | Legacy Webpack mock stub (`export default {}`) replaced by `next.config.mjs` native resolve fallback (`'onnxruntime-node': false`). | 0 static or dynamic imports across codebase; unreferenced in `package.json`, `tsconfig.json`, `jest.config.ts`, and `next.config.mjs`. | None |

---

## 2. GENERATED ARTIFACTS

Files or directories created by build tools, transpilers, or test runners that can be regenerated on demand.

| Path | Originating Tool | Regeneration Command | Tracked in Git? | Recommended Action |
| :--- | :--- | :--- | :--- | :--- |
| `tsconfig.tsbuildinfo` | TypeScript Incremental Cache | `npx tsc --noEmit` / `npm run build` | No (in `.gitignore`) | Remove from disk (regenerates automatically on build) |
| `.next/` | Next.js Build Output & Cache | `npm run build` | No (in `.gitignore`) | Preserve for active local server, or clean prior to fresh production build |

---

## 3. TEMPORARY DEVELOPMENT ARTIFACTS

Scratch files, logs, local test scripts, and debugging leftovers.

| Path | Type | Evidence of Abandonment | Risk |
| :--- | :--- | :--- | :--- |
| *None identified* | N/A | No stray `*.tmp`, `*.swp`, `*.bak`, or abandoned scratch test scripts found. | None |

---

## 4. DUPLICATES

Files with identical SHA-256 hashes or identical contents located across multiple paths.

| Original / Authoritative Path | Duplicate Path(s) | File Size | Recommended Action |
| :--- | :--- | :--- | :--- |
| `public/models/Xenova/all-MiniLM-L6-v2/vocab.txt` | `public/models/Xenova/ms-marco-MiniLM-L-6-v2/vocab.txt` | 231.5 KB | **KEEP BOTH**. These are independent model subdirectories required by Transformers.js for the embedding model and cross-encoder reranker respectively. Removing either breaks that model's tokenizer. |
| `public/models/Xenova/all-MiniLM-L6-v2/special_tokens_map.json` | `public/models/Xenova/ms-marco-MiniLM-L-6-v2/special_tokens_map.json` | 125 B | **KEEP BOTH**. Independent model configuration files. |
| `.husky/_/*` | Multiple hook templates | ~39 B | **KEEP ALL**. Standard internal templates generated by Husky v9. |

---

## 5. OBSOLETE FILES

Files belonging to removed features, deprecated architectures, or old migrations that are verified unreferenced.

| Path | Former Purpose | Verification Method | Action |
| :--- | :--- | :--- | :--- |
| `src/lib/onnxruntime-node-mock.js` | Webpack alias stub for ONNX Node runtime | Global grep yields 0 occurrences; webpack fallback handled by boolean in `next.config.mjs` | Delete |

---

## 6. REVIEW REQUIRED (Ambiguous Candidates)

Files where evidence is inconclusive, potentially dynamically loaded, or require developer confirmation. **DO NOT DELETE UNTIL REVIEWED.**

| Path | Observations / Context | Potential Risk | Questions for Reviewer |
| :--- | :--- | :--- | :--- |
| `venv/` (719.11 MB, ~20,440 files) | Python 3.11 virtual environment containing PyTorch, Transformers, AirLLM, etc. Untracked by Git (in `.gitignore`). The project has transitioned 100% to client-side TypeScript + in-browser WebLLM (`@mlc-ai/web-llm`) and has 0 Python scripts. | Low risk to project code, but deletes the developer's local Python environment if they still use it for out-of-band experimentation. | Can the `venv/` directory be safely deleted from the workspace to reclaim ~719 MB of disk space? |

---

## 7. EXPLICITLY PROTECTED (KEEP)

Critical repository files verified to be essential for runtime, security, compliance, testing, or tooling:

- **Source Control & Setup**: `.git/`, `.gitignore`, `.husky/`, `setup.bat`, `start.bat`, `SETUP_GUIDE.txt`
- **Configuration**: `package.json`, `package-lock.json`, `tsconfig.json`, `next.config.mjs`, `postcss.config.js`, `tailwind.config.js`, `.eslintrc.json`, `jest.config.ts`, `jest.setup.ts`, `the_archive.code-workspace`
- **Application Source**: `src/app/`, `src/components/`, `src/hooks/`, `src/lib/` (`constants.ts`, `sanitize.ts`, `validation.ts`), `src/services/`, `src/types/`, `src/workers/`
- **Electron Integration**: `main/index.js`, `main/preload.js`
- **Models & Assets**: `public/pdf.worker.min.mjs`, `public/models/Xenova/*`
- **Tests & Mocks**: `__tests__/*` (15 test suites, 106 tests), `__mocks__/*`
- **Documentation**: `README.md`, `CONTEXT.md`, `CONTRIBUTING.md`, `docs/adr/*`
